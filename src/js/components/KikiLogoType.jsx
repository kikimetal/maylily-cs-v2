import React from 'react'

// components
import KikiStar from "./KikiStar"

const KikiLogoType = ({ spin }) => (
  <strong className="KikiLogoType">KI<KikiStar spin={spin} />IMETAL</strong>
)

export default KikiLogoType
